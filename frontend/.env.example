# Backend API base URL. Leave unset for local dev (uses the Vite proxy to localhost:3001).
# In production (Render), set this to your deployed backend + /api/v1.
VITE_API_URL=https://billing-software-2g9k.onrender.com/api/v1
